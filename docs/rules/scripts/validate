#!/bin/bash
# Shortcut to run validation from project root

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the validation through dws
exec "$SCRIPT_DIR/dws" validate "$@"